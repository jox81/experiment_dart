name: 'webgl'
version: 0.0.5
description: Building a webgl API
author: jox81 <info@3d-jox.be>
homepage: https://www.3d-jox.be

environment:
  sdk: '>=2.0.0-dev.58.0 <2.0.0'

dependencies:
  resource: "^2.1.4"
  vector_math: '^2.0.5'
  gltf:
#    path: ../gltf
    git: git://github.com/KhronosGroup/glTF-Validator.git
  datgui: "^0.2.0+1"
  logging: "^0.11.3+1"
  stack_trace: "^1.9.2"
  reflectable:
      git: https://github.com/dart-lang/reflectable.git

dev_dependencies:
  build_runner: ^0.8.8
  build_web_compilers: ^0.4.0
  build_test: ^0.10.2
  test: "^0.12.38"

# https://webdev-dartlang-org-dev.firebaseapp.com/tools/webdev#build

# 1 - launch reflectable builder if needed
# >> dart tool/builder.dart web/*.dart
# va générer main.reflectable.dart

# 2 - serve
# >> webdev serve
# va générer main.template.reflectable.dart
# va render disponible http://localhost:8080